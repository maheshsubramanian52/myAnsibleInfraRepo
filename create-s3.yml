---
 - name:  provisioning S3 Bucket using Ansible playbook
   hosts: webservers
   #connection: local
   gather_facts: False
   tags: provisioning
   become: True

   tasks:
     - name: create S3 bucket
       amazon.aws.s3_bucket:
         name: myansibles3bucketmahesh123
         state: present
         #region: us-east-1
         #versioning: yes
         #tags:
           #name: myansiblebucket
           #type: example
       #register: s3_url

     #- name: Display s3 url
       #debug: var=s3_url
